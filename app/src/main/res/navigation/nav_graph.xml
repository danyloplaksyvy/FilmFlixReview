<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/activity_main">

    <fragment
        android:id="@+id/activity_main"
        android:name="ua.digijed.filmflixreview.MainActivity"
        tools:layout="@layout/activity_main" >

        <action
            android:id="@+id/action_MainActivity_to_SignInActivity"
            app:destination="@id/SignInActivity" />
    </fragment>
    <fragment
        android:id="@+id/SignInActivity"
        android:name="ua.digijed.filmflixreview.MoviesActivity"

        tools:layout="@layout/activity_movies" >

    </fragment>
</navigation>